server:
  port: 8848
  tomcat:
    basedir: logs
  error:
    include-message: always
db:
  num: 1
  user: ${MYSQL_USER:root}
  password: ${MYSQL_PWD:root}
  url:
    0: jdbc:mysql://127.0.0.1:3306/nacos?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  pool:
    config:
      connectionTimeout: 30000
      validationTimeout: 10000

nacos:
  core:
    auth:
      server:
        identity:
          key: test9090
          value: 2HujQv5fY0UjN6UXp4+Q91AhqyJFoVm9J5p/Tv3Uzq0=
      system.type: nacos
      plugin.nacos.token.secret.key: +1mbdX9+0/OlCCjfSschmcCP58Ynts8ll6WQb7J43NU=
  security:
    ignore:
      urls: /actuator/**,/,/error,/**/*.css,/**/*.js,/**/*.html,/**/*.map,/**/*.svg,/**/*.png,/**/*.ico,/console-fe/public/**,/v1/auth/**,/v1/console/health/**,/actuator/**,/v1/console/server/**

spring:
  application:
    name: @project.artifactId@
  datasource:
    platform: mysql  #这个过期属性不能修改，nacos 代码对此有硬编码
  security:
    enabled: true
  boot: # 接入 spring boot admin
    admin:
      client:
        url: http://127.0.0.1:5001
        username: root
        password: root
        instance:
          service-host-type: ip

useAddressServer: true

management:
  endpoints:
    web:
      exposure:
        include: '*'
  metrics:
    export:
      influx:
        enabled: false
      elastic:
        enabled: false
